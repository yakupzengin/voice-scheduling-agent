# -------------------------------------------------------
# Voice Scheduling Agent — Environment Variables
# Copy this file to .env and fill in real values.
# Never commit .env to source control.
# -------------------------------------------------------

# Server
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# Google OAuth2
# Create credentials at: https://console.cloud.google.com/apis/credentials
# Authorised redirect URI must match exactly
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

# Vapi
# VAPI_PUBLIC_KEY  — browser-safe public key: Vapi dashboard → Account → Public Key
#                    Used as the Vapi constructor argument: new Vapi(PUBLIC_KEY)
#                    Safe to expose in HTML — it is NOT your private API key.
# VAPI_ASSISTANT_ID — the assistant to start: Vapi dashboard → Assistants → copy ID
#                    Passed to vapi.start(ASSISTANT_ID, { metadata: ... })
VAPI_PUBLIC_KEY=your-vapi-public-key
VAPI_ASSISTANT_ID=your-vapi-assistant-id

# SQLite database path
# Local: leave as default
# Railway: set to /data/tokens.db (Railway volume must be mounted at /data)
DB_PATH=./tokens.db
